version: 2
ethernets:
  eth0:
    match: { name: enp5* }
    dhcp4: true
    # dhcp4-overrides:
    #     use-routes: false
    # routes:
    #   - to: default
    #     via: ${proxy_ip}
    #     on-link: true
    # nameservers:
    #   addresses: ${nameservers}
    #   search: ${search_domains}
  eth1:
    match: { name: enp6* }
    dhcp4: false
  eth2:
    match: { name: enp7* }
    dhcp4: false
  eth3:
    match: { name: enp8* }
    dhcp4: false
